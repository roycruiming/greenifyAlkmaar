using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface DragPuzzle 
{
    // Start is called before the first frame update

    void EndDragAction(DragAndDrop gameObject);
}
